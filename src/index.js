import React from 'react';
import ReactDOM from 'react-dom';
import LikeButton from './LikeButton/LikeButton';

const root = document.querySelector('#root');
ReactDOM.render(React.createElement(LikeButton), root);
